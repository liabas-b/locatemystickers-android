<?xml version="1.0" encoding="utf-8"?>

<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:lms="http://schemas.android.com/apk/res-auto"
             android:layout_width="fill_parent"
             android:layout_height="fill_parent">
    <LinearLayout
            android:id="@+id/back"
            android:tag="back"
            style="@style/ListBackContent"
            android:gravity="center_horizontal|center_vertical">
        <ImageButton
            android:id="@+id/btnShare"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:background="@color/background_list_back"
            android:src="@drawable/btn_refresh" />

        <ImageButton
            android:id="@+id/btnGlobe"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:background="@color/background_list_back"
            android:src="@drawable/btn_globe" />

        <ImageButton
            android:id="@+id/btnIconLogo"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:background="@color/background_list_back"
            android:src="@drawable/btn_lms" />

        <ImageButton
            android:id="@+id/btnEditDelete"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:background="@color/background_list_back"
            android:src="@drawable/btn_editdelete" />

        <ImageButton
            android:id="@+id/btnMult"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:background="@color/background_list_back"
            android:src="@drawable/btn_mult" />

    </LinearLayout>

    <RelativeLayout
            android:orientation="vertical"
            android:id="@+id/front"
            android:tag="front"
            style="@style/ListFrontContent">
        <TextView
            android:id="@+id/txtUser"
            android:textColor="@android:color/background_dark"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            />
        <ImageView
            android:id="@+id/imgSticker"
            android:layout_below="@id/txtUser"
            android:src="@drawable/lms_qrcode"
            android:layout_width="20dp"
            android:layout_height="20dp"/>
        <TextView
            android:id="@+id/txtCounterStickers"
            android:textColor="@android:color/background_dark"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toRightOf="@id/imgSticker"
            android:layout_marginLeft="5dp"
            android:layout_below="@+id/txtUser"/>
    </RelativeLayout>
</FrameLayout>